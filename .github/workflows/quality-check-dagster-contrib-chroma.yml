name: quality-check-dagster-contrib-chroma
on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'libraries/dagster-contrib-chroma/**'

jobs:
  check:
    uses: ./.github/workflows/template-quality-check.yml
    with:
      working_directory: ./libraries/dagster-contrib-chroma
